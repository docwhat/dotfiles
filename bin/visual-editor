#!/usr/bin/env zsh

if (( $+commands[nvr] )); then
  nvr -s --nostart --remote-wait "$@"
elif (( $+commands[mvim] )); then
  mvim -f "$@"
elif (( $+commands[gvim] )); then
  gvim "$@"
else
  "$EDITOR" "$@"
fi

# EOF
