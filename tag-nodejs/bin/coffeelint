#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/coffeelint' ]; then
	exec 'node_modules/.bin/coffeelint' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/coffeelint/node_modules/.bin/coffeelint" "$@"
fi
