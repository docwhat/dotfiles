#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/neovim-node-host' ]; then
	exec 'node_modules/.bin/neovim-node-host' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/neovim/node_modules/.bin/neovim-node-host" "$@"
fi
