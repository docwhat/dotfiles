#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/jshint' ]; then
	exec 'node_modules/.bin/jshint' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/jshint/node_modules/.bin/jshint" "$@"
fi
