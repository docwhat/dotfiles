#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/write-good' ]; then
	exec 'node_modules/.bin/write-good' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/write-good/node_modules/.bin/write-good" "$@"
fi
