#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/tern' ]; then
	exec 'node_modules/.bin/tern' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/tern/node_modules/.bin/tern" "$@"
fi
