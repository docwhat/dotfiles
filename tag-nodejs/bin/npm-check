#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/npm-check' ]; then
	exec 'node_modules/.bin/npm-check' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/npm-check/node_modules/.bin/npm-check" "$@"
fi
