#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/htmlhint' ]; then
	exec 'node_modules/.bin/htmlhint' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/htmlhint/node_modules/.bin/htmlhint" "$@"
fi
