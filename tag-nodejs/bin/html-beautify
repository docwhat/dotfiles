#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/html-beautify' ]; then
	exec 'node_modules/.bin/html-beautify' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/js-beautify/node_modules/.bin/html-beautify" "$@"
fi
