#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/gatsby' ]; then
	exec 'node_modules/.bin/gatsby' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/gatsby-cli/node_modules/.bin/gatsby" "$@"
fi
