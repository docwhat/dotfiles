#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/gatsby-dev' ]; then
	exec 'node_modules/.bin/gatsby-dev' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/gatsby-dev-cli/node_modules/.bin/gatsby-dev" "$@"
fi
