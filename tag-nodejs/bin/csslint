#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/csslint' ]; then
	exec 'node_modules/.bin/csslint' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/csslint/node_modules/.bin/csslint" "$@"
fi
