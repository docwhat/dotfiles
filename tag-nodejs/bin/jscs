#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/jscs' ]; then
	exec 'node_modules/.bin/jscs' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/jscs/node_modules/.bin/jscs" "$@"
fi
