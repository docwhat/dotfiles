#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/eslint' ]; then
	exec 'node_modules/.bin/eslint' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/eslint/node_modules/.bin/eslint" "$@"
fi
