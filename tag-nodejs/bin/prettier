#!/usr/bin/env zsh
set -eu
if [ -f 'package.json' ] && [ -x 'node_modules/.bin/prettier' ]; then
	exec 'node_modules/.bin/prettier' "$@"
else
	exec "${HOME}/.local/libexec/nodejs-cli-tools/prettier/node_modules/.bin/prettier" "$@"
fi
