#!/usr/bin/env zsh
# vi: ft=zsh :

set -euo pipefail

DOTFILES_DIR=~/.dotfiles
source "${DOTFILES_DIR}/local/include/hook-helpers.zsh"

npm install --global \
  'gulpjs/gulp#4.0' \
  eslint \
  csslint \
  htmlhint \
  coffeelint \
  stylelint \
  write-good

npm upgrade --global

# EOF
